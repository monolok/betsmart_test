<h3>Surebet comming up: <%= @game_data.size%></h3>
<%# console%>
<div class="bs-example" data-example-id="hoverable-table">
    <table class="table table-hover">
      <tbody>
        <% if @game_data.empty? %>
    	    <tr>
      			<td>Pas de surebet en cours</td>
            <%# @game_data%>
          </tr>
        <%else%>
  	      <% @game_data.each do |game|%>
  	        <tr>
  				    <td><%= game%></td>
  	        </tr>
  	       <%end%>
         <%end%>
      </tbody>
    </table>
</div>
 
 <%# @game_data[0]%>